# Scheduled tasks.

PATH = /usr/bin:/usr/local/bin:/usr/local/sbin:/home/ggp/.local/bin

# options
!mail(false),nolog(true),bootrun(true)

# check for RSS updates
@ 10 sfeed_update

# update manual page index cache
@ 1h mandb
